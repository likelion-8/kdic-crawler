"""복합 질문 분해 — 한 문장에 여러 정보 요구가 섞인 질문을 독립된 하위 질문으로 쪼갠다.

⚠️ 현재 서비스 경로에서는 타지 않는다. 2026-08-09에 분해와 intent 분류를 한 콜로 합친
query_planner.plan_query()로 넘어가면서, 이 모듈은 USE_QUERY_PLANNER=False 로 되돌렸을 때만
쓰는 폴백이 됐다(교체 근거: docs/query_planner_model_comparison.md — joint 정확도 79→89%,
질문당 토큰 -73%). 코드는 롤백 대비로 남겨 둔다.

"신청 방법과 필요한 서류, 처리 기간을 알려주세요" 처럼 같은 업무 안에서도 서로 다른 근거
(문서)가 필요한 요구가 여러 개 붙어 있으면 단일 질의로는 top-k 검색이 한두 개 요구에만
근거를 맞히고 나머지가 밀려난다(docs/worklog/0729.md 3항). 규칙 기반(접속사·쉼표 분리)은 "예금보험금
이란 무엇인가요?" 같은 단일 질문도 오분해할 위험이 커서, LLM에게 판단과 분해를 같이 맡긴다.

분해된 하위 질문은 각각 retrieval.route_search_chunks()에 독립적으로 넣어 검색한다
(이 파일은 분해까지만 책임지고, 검색·조립은 pipeline.py 쪽에서 연결한다).
"""
import re

from llm_client import call_hyperclova

SYSTEM_INSTRUCTION = """당신은 예금보험공사(KDIC) 챗봇의 질문 분해기입니다. 사용자 질문 하나를 받아
독립적으로 검색·답변할 수 있는 하위 질문들로 나누는 것이 역할입니다. 질문에 답변하는 역할이
아닙니다 — 절대 정의나 설명, 사실 정보를 출력에 쓰지 마세요.

다음 원칙을 반드시 지키세요:
1. 질문이 실제로 서로 다른 정보 요구(예: 신청 방법 / 필요 서류 / 처리 기간, 또는 서로 다른 제도에
   대한 질문)를 담고 있을 때만 나누세요. 표현이 길거나 문장이 복잡해도 요구가 하나뿐이면 나누지
   마세요. "OO는 어떻게 하나요?" 처럼 요구가 하나뿐인 절차 질문도 나누지 마세요 — "서류는?",
   "기간은?" 같이 질문에 없는 하위 항목을 임의로 만들어내면 안 됩니다.
2. 나눌 필요가 없으면(단일 질문이면) 입력 질문을 단어 하나도 바꾸지 말고 그대로 한 줄만
   출력하세요. 답변을 쓰거나, 질문을 풀어 설명하거나, 다른 표현으로 바꿔 쓰지 마세요.
3. 나눌 때는 각 하위 질문이 원래 문맥 없이도 무엇을 묻는지 알 수 있도록 주어(제도·업무명 등)를
   생략하지 말고 채워 넣으세요. 원래 질문에 실제로 적힌 요구만 대상으로 삼고, 없던 요구를
   추가하거나 짐작하지 마세요.
4. 하위 질문은 원래 질문이 요구한 정보를 하나도 누락하지 않는 최소 개수로 나누세요.
5. 출력은 각 줄에 하위 질문 하나씩만 쓰세요. 번호, 불릿(-, *, 1.), 따옴표, 설명 문구를 절대
   붙이지 마세요 — 파싱 코드가 줄바꿈만 보고 나눕니다.
6. "방법"과 "절차", "기한"과 "마감 기한"처럼 표현만 다르고 실제로는 같은 정보를 가리키는
   단어가 접속어로 이어져 있으면 나누지 마세요 — 판단 기준은 단어 개수가 아니라 서로 다른
   사실(예: 서류 vs 기간처럼 실제로 답이 갈리는 내용)을 요구하는지입니다.
7. 서로 다른 정보 요구가 실제로 있다면, 원 문장에 접속어나 마침표 구분이 없거나 띄어쓰기가
   붙어 있어도, 또는 한 문장에 여러 서술어가 이어져 있어도 요구 개수만큼 반드시 별도 줄로
   나누세요. 표현이 매끄럽다고 한 줄에 몰아 쓰지 마세요."""

FEW_SHOT_EXAMPLES = [
    {
        # 단일 질문(정의형) — 나누지 않고, 답변하지도 않음
        "question": "예금보험금이라는 게 정확히 뭔가요?",
        "answer": "예금보험금이라는 게 정확히 뭔가요?",
    },
    {
        # 단일 질문(절차형, 요구 1개) — "서류는?", "기간은?"을 임의로 만들어내면 안 되는 사례
        "question": "착오송금 반환 신청은 어떻게 하나요?",
        "answer": "착오송금 반환 신청은 어떻게 하나요?",
    },
    {
        # 동의어 함정 — "방법"과 "절차"는 같은 요구라 나누면 안 되는 사례(실패 재현 케이스)
        "question": "예금보험금 신청 방법하고 절차 좀 알려주세요.",
        "answer": "예금보험금 신청 방법하고 절차 좀 알려주세요.",
    },
    {
        # 같은 업무 안 복합 요구 — docs/worklog/0729.md 예시
        "question": "예금보험금 신청 방법과 필요한 서류, 처리 기간을 알려주세요.",
        "answer": (
            "예금보험금은 어떻게 신청하나요?\n"
            "예금보험금 신청에 필요한 서류는 무엇인가요?\n"
            "예금보험금 처리 기간은 얼마나 걸리나요?"
        ),
    },
    {
        # 서로 다른 업무 복합 — docs/worklog/0729.md 예시
        "question": "예금보험금 신청 방법과 착오송금 반환 신청 조건을 알려주세요.",
        "answer": (
            "예금보험금은 어떻게 신청하나요?\n"
            "착오송금 반환 신청 조건은 무엇인가요?"
        ),
    },
    {
        # 접속어·띄어쓰기 없이 붙여 쓴 복합 요구 — 실패 재현 케이스(한 줄로 합치면 안 됨)
        "question": "착오송금신청서류랑 처리기간좀 알려주세요오",
        "answer": (
            "착오송금 신청에 필요한 서류는 무엇인가요?\n"
            "착오송금 신청 처리 기간은 얼마나 걸리나요?"
        ),
    },
]


def _format_examples():
    return "\n\n".join(f"질문: {ex['question']}\n출력:\n{ex['answer']}" for ex in FEW_SHOT_EXAMPLES)


def _build_prompt(query):
    human = (
        f"{_format_examples()}\n\n"
        "--- 아래는 실제 질문입니다 ---\n\n"
        f"질문: {query}\n출력:"
    )
    return [("system", SYSTEM_INSTRUCTION), ("human", human)]


# 2026-08-04(docs/pipeline_issue_history.md 이슈 6): few-shot 프롬프트가 "질문: ...\n출력:" 형식을
# 쓰다 보니, 드물게 LLM이 실제 하위 질문 대신 이 틀 자체("질문:", "출력:" 한 줄)를 그대로
# 따라 써서 응답에 섞어 보낸다. decompose_query()가 줄바꿈만 보고 나누기 때문에 이런 줄이
# 그대로 "하위 질문"으로 취급돼 pipeline.py에 넘어가면, "출력:" 같은 무의미한 문자열이 질문
# 취급을 받아 근거 없음 답변("제공해주신 자료에... 답변 못 드립니다")이 섞여 나온다.
# 재현율은 낮지만(직접 재현 시도 5/5는 정상), 방어 코드 자체엔 비용이 없으므로 필터링한다.
_PROMPT_ARTIFACT_RE = re.compile(r"^(질문|출력)\s*[:：]")


def decompose_query(query):
    """질문 하나 -> 하위 질문 문자열 리스트. 나눌 필요가 없으면 [query]와 동일한 내용의
    한 원소 리스트가 된다. LLM 출력 파싱에 실패하거나(빈 응답 등) 명백히 잘못됐으면,
    또는 call_hyperclova 호출 자체가 실패하면(타임아웃 등) 분해하지 않은 원본 질문
    그대로 담아 반환한다 — 분해 실패가 검색 자체를 막으면 안 된다.

    ⚠️ 비용 집계 주의(2026-08-29): 이 콜은 span 이름이 call_hyperclova 라
    observability.SERVING_GENERATION_NAMES 에 안 잡힌다(그 이름은 평가·CLI 도 쓴다).
    지금은 새는 게 없다 — 이 함수는 use_query_planner=False 일 때만 도는데 기본값이 True 고
    그 파라미터는 2026-08-26 에 관리자 화면에서 빠졌다. DB 에 옛 파라미터 행이 남아 False 로
    읽히면 이 HCX 비용이 대시보드 밖으로 나간다. 그 경로를 되살릴 때는 재생성과 같은 방식으로
    (llm_client.regenerate_hyperclova 참고) 전용 span 이름을 만들고 두 목록에 더할 것."""
    try:
        response = call_hyperclova(_build_prompt(query))
    except Exception:
        return [query]
    sub_queries = [line.strip() for line in response.strip().splitlines()
                   if line.strip() and not _PROMPT_ARTIFACT_RE.match(line.strip())]
    return sub_queries if sub_queries else [query]


if __name__ == "__main__":
    samples = [
        "예금보험금이라는 게 정확히 뭔가요?",
        "착오송금 반환 신청은 어떻게 하나요?",
        "예금보험금 신청 방법과 필요한 서류, 처리 기간을 알려주세요.",
        "예금보험금 신청 방법과 착오송금 반환 신청 조건을 알려주세요.",
        "잘못 보낸 돈을 돌려받으려면 어떤 서류가 필요하고 신청 기한은 언제까지인가요?",
    ]
    for q in samples:
        print(f"질문: {q}")
        for i, sq in enumerate(decompose_query(q), 1):
            print(f"  [{i}] {sq}")
        print()
