# 예솜24 관리자 패널 최종 QA 보고서

- 점검일: 2026-08-25
- 대상: `http://localhost:5173/admin`
- 계정: `admin@demo`, `ADMIN`
- 백엔드: `http://localhost:8000`
- 상태 확인: `GET /api/health` 200, `maintenance=false`
- 프론트 설정: `VITE_ENABLE_MSW=false`, `VITE_API_BASE=http://localhost:8000`
- 저장소 변경 여부: 변경 없음

## 1. 점검 범위

### 관리자 화면

- 대시보드
- 지식베이스 목록, 필터, 상세, 삭제
- 신규 URL 추가, 미리보기, 적재
- 파이프라인, 변경 감지, 재수집, 재적재, 실패 상세
- 대화 로그, 필터, 상세, 처리 완료, 내보내기
- 평가셋, 평가 결과, 평가 문항 편집
- RAG 파라미터
- 프롬프트, 금칙어, 마스킹 규칙
- 운영 정책, 사용량 제한, 캐시, 추천 질문
- 보안과 권한, 계정, 세션, 로그인 실패
- 활동 로그, 상세, 필터, 내보내기
- 로그인, 계정 초대, 비밀번호 재설정

### 챗봇과 기준 문서

- 챗봇 홈
- FAQ 질문
- 정체성 질문
- 범위 외 질문
- 모호한 질문과 업무 되묻기
- 복합 질문
- 새 대화
- Figma 공통 셸과 관리자 IA 비교

## 2. 최종 판정

ADMIN 단일 계정과 실제 8000번 백엔드 연동 기준으로 주요 메뉴, 라우트, 핵심 사용자 흐름을 전수 확인했다.

실제 백엔드 상태에서도 다음 결함을 재현했다.

- 파이프라인 실패 상세 크래시
- 사이드바 클릭 후 URL과 화면 상태 불일치
- 파이프라인 작업의 QUEUED 고착 가능성
- 실제 파일을 만들지 않는 로그 내보내기
- 실제 메일을 발송하지 않는 초대와 비밀번호 재설정

---

# 3. 우선순위별 이슈

## [P0] 파이프라인 실패 상세 진입 시 전체 화면 크래시

### 대상

`/admin/pipeline`

### 재현 조건

1. 파이프라인 실행 이력에서 실패 상태 행을 찾는다.
2. `[상세]`를 클릭한다.
3. 실패 상세 모달 대신 React 오류 화면이 표시된다.
4. 새로고침하면 정상 화면으로 돌아온다.

### 실제 증상

```text
Unexpected Application Error!
Cannot read properties of undefined (reading 'replace')
at JobFailureDetail
```

실제 백엔드 연동 상태에서 재현했다.

### 근본 원인

- `web/src/routes/admin/pipeline/JobFailureDetail.tsx:57`

```ts
const reasonText = JOB_ERROR_MESSAGE[code].replace('{단계}', stage)
```

- 프론트가 정의한 오류 코드는 다음 5종뿐이다.

```ts
STAGE_TIMEOUT
JOB_TIMEOUT
SOURCE_ERROR
RESOURCE_ERROR
INTERNAL
```

- 하지만 실제 워커는 다음 값을 기록한다.

- `src/worker.py:579`

```py
error={"code": "STAGE_FAILED", "stage": exc.stage, "detail": exc.detail}
```

`STAGE_FAILED`는 프론트 오류 코드 목록과 매핑 테이블에 없으므로 `JOB_ERROR_MESSAGE[code]`가 `undefined`가 된다.

또한 `api/schemas/pipeline.py:32`의 오류 코드는 단순 `str`이라 백엔드에서 계약 오류를 차단하지 못한다.

### 수정 방향

1. 서버에서 `STAGE_FAILED`를 허용된 코드 중 하나로 정규화한다.
2. 또는 프론트에 `STAGE_FAILED` 매핑을 추가한다.
3. 프론트에서도 알 수 없는 코드가 들어오면 `INTERNAL` 문구로 폴백한다.
4. API 응답 계약에 오류 코드 enum 검증을 추가한다.

### 완료 기준

- 모든 FAILED 작업의 상세가 정상 렌더링된다.
- 알 수 없는 오류 코드가 들어와도 화면이 크래시하지 않는다.
- 오류 코드, 실패 단계, 오류 사유, 자동 재시도, 인덱스 영향이 모두 표시된다.
- 워커 실패 응답과 프론트 `JobErrorCode` 계약 테스트가 추가된다.

---

## [P1] 사이드바 클릭 후 URL과 실제 화면 상태 불일치

### 대상

관리자 전체 메뉴

### 재현 조건

1. `/admin/logs`에서 시작한다.
2. 사이드바의 `파이프라인`을 클릭한다.
3. 화면 내용은 파이프라인으로 바뀐다.
4. 다시 `지식베이스`를 클릭한다.

### 실제 증상

- 화면은 지식베이스로 바뀐다.
- 브라우저 URL은 계속 `/admin/pipeline`에 남는다.
- 새로고침하면 URL 기준으로 파이프라인 화면이 다시 열린다.
- 뒤로가기와 북마크 진입도 잘못된 화면을 가리킬 수 있다.

### 확인 내용

- `AdminLayout.tsx:207-214`의 `NavLink`와 href는 정상이다.
- `web/src/app/router.tsx`의 라우트 정의도 정상이다.
- 따라서 단순 메뉴 설정 문제가 아니라 런타임에서 history push와 화면 렌더링이 어긋나는 문제로 보인다.

### 수정 방향

1. 클릭 시 `router.state.location.pathname`과 브라우저 `location.pathname`을 함께 로깅한다.
2. React Router 인스턴스 중복 생성 여부를 확인한다.
3. 화면 제목과 콘텐츠를 별도 상태로 관리하지 말고 라우터 location에서만 파생한다.
4. 사이드바 E2E 테스트에 URL 검증을 추가한다.

### 완료 기준

- 메뉴 클릭 후 URL, active 메뉴, 화면 콘텐츠가 항상 같은 경로를 가리킨다.
- 새로고침, 뒤로가기, 앞으로가기, 직접 URL 진입이 모두 같은 결과를 낸다.
- 모든 관리자 메뉴에 대해 `href === 실제 pathname` 검증이 통과한다.

---

## [P1] 파이프라인 워커와 배치 실행 주체가 서비스 구성에 연결되지 않음

### 대상

파이프라인, 변경 감지, 재수집, 재적재, 평가 재측정

### 실제 증상

현재 파이프라인에 다음 작업이 대기 상태로 남아 있다.

```text
08-24 15:38 변경 감지
상태: 대기
```

워커가 실행되지 않으면 작업이 `QUEUED`에 계속 남을 수 있다.

### 근본 원인

- 워커 코드는 `src/worker.py`에 존재한다.
- 하지만 수동 실행 방식만 제공한다.

```text
python src/worker.py
python src/worker.py --once
```

- `api/main.py`의 lifespan에는 워커 자동 기동 코드가 없다.
- 저장소 compose 구성에도 워커 서비스가 없다.
- 배치 스케줄러도 별도로 확인되지 않는다.
- 활동 로그의 보관 예정 건수는 계산하지만 실제 자동 파기 실행 주체는 없다.

### 영향

- 전체 재수집
- 선택 재수집
- 재적재
- 변경 감지
- 평가셋 재측정
- 지식베이스 신규 적재 후 REINDEX

위 작업이 별도 워커가 실행되지 않는 환경에서 영구 대기할 수 있다.

### 수정 방향

1. 워커를 Docker 서비스, supervisor, systemd 또는 launchd로 상시 실행한다.
2. 워커 heartbeat와 마지막 처리 시각을 관리자 화면에 표시한다.
3. 일정 시간 QUEUED에 머물면 워커 미실행 경고를 표시한다.
4. 변경 감지와 보관기간 파기용 스케줄러를 별도로 연결한다.
5. 재시작 후 QUEUED 작업을 안전하게 재처리하는 정책을 추가한다.

### 완료 기준

- 작업 생성 후 정상 환경에서 3초 안에 RUNNING으로 전환된다.
- 워커가 중단되면 관리자 화면에 명확한 경고가 표시된다.
- 워커 재시작 후 기존 QUEUED 작업이 중복 없이 처리된다.
- 정기 변경 감지와 보관기간 파기가 실제 지정된 시각에 실행된다.

---

## [P1] 계정 초대와 비밀번호 재설정 메일이 실제 발송되지 않음

### 대상

보안과 권한, 로그인, 계정 초대, 비밀번호 재설정

### 재현 조건

1. 로그인 화면에서 비밀번호 재설정을 선택한다.
2. 이메일을 입력하고 `[재설정 링크 보내기]`를 클릭한다.
3. 화면에는 발송 완료 문구가 표시된다.
4. 실제 메일 발송 경로는 없다.

### 근본 원인

- `api/routers/admin_auth.py:755-795`
- 토큰은 생성되지만 SMTP 또는 메일 제공자 연동이 없다.
- 현재 토큰은 서버 로그에 링크 형태로만 기록된다.
- 계정 초대도 계정과 토큰만 생성한다.
- `PasswordResetPanel.tsx:168-176`은 발송 완료로 안내한다.

### 영향

- 신규 관리자 초대 불가
- 기존 관리자 비밀번호 분실 시 셀프 복구 불가
- 서버 로그에 재설정 토큰이 남을 위험

### 수정 방향

1. SMTP 또는 외부 메일 서비스 연동
2. 메일 발송 outbox와 실패 재시도 추가
3. 토큰 원문 로그 제거
4. 발송 성공 여부와 사용자 안내 문구 분리
5. 초대와 재설정 링크의 만료, 1회 사용, 재요청 정책 테스트

### 완료 기준

- 초대 메일과 재설정 메일이 실제 수신된다.
- 메일 링크로 비밀번호 설정이 완료된다.
- 토큰 원문이 애플리케이션 로그에 남지 않는다.
- 존재하지 않는 이메일 요청은 현재처럼 계정 존재 여부를 노출하지 않는다.

---

## [P1] 로그 내보내기가 접수증만 반환하고 실제 파일을 생성하지 않음

### 대상

활동 로그, 대화 로그

### 재현 조건

1. 로그 화면에서 필터를 적용한다.
2. `[내보내기]`를 클릭한다.
3. 활동 로그는 사유를 입력하고 확인한다.
4. 대화 로그는 내보내기를 실행한다.
5. 토스트는 표시되지만 다운로드 파일은 생성되지 않는다.

### 근본 원인

- `api/routers/admin_activity.py:296-342`
- `api/routers/admin_logs.py:624-667`

두 API 모두 다음 정보만 반환한다.

```json
{
  "export_id": "...",
  "status": "QUEUED",
  "estimated_rows": 760
}
```

실제 파일 생성, 다운로드 URL, 완료 상태를 만드는 로직은 없다.

### 수정 방향

1. CSV 또는 JSON export worker 추가
2. export 상태를 `QUEUED`, `RUNNING`, `SUCCESS`, `FAILED`로 관리
3. 완료 후 다운로드 URL 제공
4. 다운로드 URL 만료와 권한 검증 추가
5. 필터 조건과 실제 파일 건수 일치 여부 검증

### 완료 기준

- 현재 필터 결과와 동일한 데이터가 파일로 생성된다.
- 파일 다운로드가 실제로 동작한다.
- 실패 시 사용자에게 실패 원인이 표시된다.
- 내보내기 행위와 파일 생성 결과가 활동 로그에 남는다.

---

## [P2] 파이프라인 6단계와 7단계 문구 불일치

### 증상

화면 단계는 다음 7개다.

```text
수집, 변환, 청킹, 검증, 게이트, 색인, 반영
```

하지만 화면 안내 문구와 일부 백엔드 주석은 아직 다음처럼 표시한다.

```text
6단계까지 모두 통과하면
```

### 근거

- `web/src/lib/constants.ts:34`: 7단계
- `src/worker.py:93`: 7단계
- `api/routers/admin_pipeline.py:32,120`: 6단계 주석
- `web/src/routes/admin/Pipeline.tsx:349,374`: 6단계 화면 문구

### 수정 방향

화면 문구, API 주석, 스키마 주석, 운영 문서를 `PIPELINE_STEPS`에서 파생하거나 7단계 기준으로 통일한다.

### 완료 기준

- 화면, 백엔드, 워커, Figma 설명이 모두 7단계로 일치한다.
- 게이트가 어느 위치에서 실행되는지 모든 화면에서 동일하게 설명한다.

---

## [P2] 운영 정책 일부가 저장만 되고 실제 집행되지 않음

### 확인된 정상 집행

- IP별 분당 요청 제한
- IP별 일일 요청 제한
- 10초 burst 제한
- 반복 위반 시 임시 차단

### 미집행 항목

- `session_per_30min`
  - 화면에서 저장과 표시 가능
  - `api/middleware.py:108-109`에서 세션별 집행은 후속 과제로 명시
- `auto_purge`
  - 정책 필드와 토글은 존재
  - 실제 자동 파기 소비자와 실행 스케줄러는 확인되지 않음

### 수정 방향

- 세션 ID를 기준으로 한 공유 저장소 기반 quota 추가
- `auto_purge`를 실제 보관기간 파기 작업과 연결
- 정책 저장 화면에 미집행 상태를 명시하거나 집행 완료 전에는 편집을 숨김

### 완료 기준

- 저장한 세션별 제한이 실제 챗봇 요청에 적용된다.
- 자동 비우기 설정이 실제 캐시 또는 보관 데이터 파기에 반영된다.
- 프로세스 여러 개를 실행해도 제한값이 공유된다.

---

## [P2] 프론트 selfcheck가 최신 업무 선택지와 불일치

### 재현

```bash
npm --prefix web run check
```

TypeScript와 대부분 selfcheck는 통과하지만 다음 assertion이 실패한다.

```text
web/src/mocks/selfcheck.ts:89
업무 선택지는 5개다

6 !== 5
```

### 근본 원인

현재 업무 선택지는 6개다.

- `web/src/lib/codes.ts:14-21`
- `src/clarify.py:46-53`

하지만 `web/src/mocks/selfcheck.ts:89`는 5개를 기대한다.

### 수정 방향

- assertion을 6개로 수정
- 가능하면 하드코딩하지 않고 `CLARIFY_OPTIONS.length` 또는 정본 상수와 비교

### 완료 기준

```bash
npm --prefix web run check
```

전체 selfcheck가 성공한다.

---

## [P2] 기본 프롬프트의 서비스명 불일치

### 증상

화면과 서비스 기준 명칭은 `예솜24`다.

하지만 코드 기본 시스템 프롬프트는 다음처럼 `예솜`만 사용한다.

- `src/prompt_builder.py:45`
- `src/prompt_builder.py:56`

### 영향

게시된 프롬프트가 없을 때 정체성 질문에서 화면 브랜드와 답변 브랜드가 달라질 수 있다.

### 수정 방향

- 기본 시스템 프롬프트와 few-shot 답변을 `예솜24` 기준으로 통일
- 정체성 질문 selfcheck 추가

### 완료 기준

- 홈 화면, 관리자 화면, 시스템 프롬프트, 챗봇 정체성 답변이 모두 동일한 서비스명을 사용한다.

---

## [P2] 챗봇 복합 질문 진행상황 표시 부족

### 확인 결과

복합 질문은 여러 하위 질문으로 분해되어 순차 처리되며 기능 자체는 정상이다.

### UX 문제

처리 중에는 단순히 "생각 중" 상태만 표시되어 다음 정보를 알기 어렵다.

- 몇 개의 하위 질문을 처리 중인지
- 현재 어느 하위 질문을 처리하는지
- 전체 예상 진행률

### 수정 방향

스트리밍 이벤트에 하위 질문 진행 상태를 추가하거나, 최소한 복합 질문 처리 중이라는 안내를 표시한다.

### 완료 기준

- 복합 질문 처리 중 사용자에게 진행 상태가 표시된다.
- 완료 전까지 입력 잠금과 중단 동작이 유지된다.
- 최종 답변과 하위 답변의 순서가 일치한다.

---

# 4. 정상 동작으로 확인한 항목

- 지식베이스 목록, 필터, 상세, 삭제 흐름
- 신규 URL 미리보기와 적재 요청
- 변경 요청 승인 후 실제 적재와 REINDEX 요청 연결
- 평가셋 목록과 페이지 크기 변경
- RAG 파라미터 초안, 평가, 상태 추적
- 프롬프트 원칙 편집과 잠금 원칙 처리
- 금칙어와 마스킹 규칙 편집
- 운영 정책 저장, 초기화, 취소
- 캐시 목록과 개별 삭제 흐름
- 추천 질문 추가, 편집, 비활성화
- 계정과 세션 현황
- 로그인 실패 내역
- 활동 로그 필터와 상세
- 비밀번호 재설정 API 경로
- 챗봇 FAQ, 정체성, 범위 외 질문
- 챗봇 복합 질문의 순차 처리
- 새 대화 버튼
- 게이트 미달 후 색인 진행 정책
- 잘못된 마스킹 정규식 저장 허용 정책

# 5. 의도된 정책으로 분류한 항목

## 마스킹 정규식 검증 실패 후 저장 허용

서버 검증은 정상적으로 오류를 반환한다.

다만 2026-08-19 정책에 따라 검증 실패를 저장 차단이 아니라 경고로 처리한다.

현재 동작은 버그가 아니라 의도된 정책이다.

개선 여지는 다음과 같다.

- 저장 전 경고를 더 눈에 띄게 표시
- 검증 실패 규칙 수를 확인 모달에 표시
- 운영 환경에서는 저장 차단 여부를 정책으로 선택 가능하게 제공

## 게이트 미달 후 색인 진행

2026-08-19 정책 변경으로 게이트는 차단 장치가 아니라 경고 장치다.

현재 화면도 게이트 미달 상태로 색인이 반영되었음을 표시한다.

# 6. Figma 비교 결과

- 관리자 공통 셸, 좌측 GNB, 상단 세션 정보, 화면 ID 표시는 기준과 대체로 일치한다.
- 지식베이스와 파이프라인의 주요 정보 구조는 Figma 기준과 일치한다.
- 설정 메뉴는 Figma의 가로 서브탭 대신 좌측 2단계 메뉴로 구현되어 있다.
  - 코드 주석상 키보드 접근성과 직접 URL 이동을 위한 구현 결정이다.
  - 제품 결정으로 유지할지 확인이 필요하다.
- 주요 문서와 구현 불일치는 파이프라인 6단계와 7단계 문구, 서비스명, 운영 자동화 범위다.

# 7. 수정 우선순위

1. P0 파이프라인 실패 상세 크래시
2. P1 실제 백엔드 워커 실행과 QUEUED 고착 방지
3. P1 사이드바 URL과 화면 상태 불일치
4. P1 계정 초대와 비밀번호 재설정 메일 발송
5. P1 로그 내보내기 실제 파일 생성
6. P2 파이프라인 6단계와 7단계 문구 통일
7. P2 운영 정책 미집행 필드 정리
8. P2 selfcheck의 5개와 6개 불일치 수정
9. P2 기본 프롬프트 서비스명 통일
10. P2 복합 질문 진행상황 UX 개선

# 8. QA 결론

ADMIN 단일 계정과 실제 8000번 백엔드 연동 기준으로 관리자 주요 메뉴, 라우트, 핵심 사용자 경로를 전수 확인했다.

최우선 수정 대상은 파이프라인 실패 상세 크래시다.

그 다음으로 실제 운영 전 반드시 확인해야 할 항목은 워커 실행 구성, 관리자 초대 및 비밀번호 재설정 메일, 로그 내보내기 파일 생성이다.

---

# 9. 조치 결과 (2026-08-25, `fix/admin-qa-2026-08-25`)

위 보고서를 코드와 대조해 처리한 결과다. **위 본문은 점검 시점 기록이라 그대로 두고, 대조로
달라진 판정만 여기 적는다.** 이메일 연동(P1)은 마감 사유로 범위 밖.

## 처리한 것

| 항목 | 조치 |
|---|---|
| P0 실패 상세 크래시 | `codes.ts` 에 `STAGE_FAILED` 추가 + `jobErrorMessage()` 로 미지 코드는 INTERNAL 문구 폴백. 목도 실제 코드로 교체. `tests/test_pipeline_error_contract.py` 가 `worker.py` ↔ `codes.ts` 대조 |
| P1 워커 미기동 | `poll_forever` 로 루프를 빼고 API lifespan 이 데몬 스레드로 상시 실행(`API_WORKER_IN_PROCESS`). 루프는 어떤 예외로도 안 죽는다 |
| P1 로그 내보내기 | `api/export_csv.py` — 접수증 대신 CSV 파일을 그 요청 안에서 만들어 내려준다. 프론트 `apiDownload` 가 저장까지 하고 토스트가 실제 건수를 말한다 |
| P2 6/7단계 문구 | 화면 문구를 `PIPELINE_STEPS.length` 에서 파생 — 단계가 또 늘어도 안 갈린다 |
| P2 `session_per_30min` | `api/ops_policy.py` 로 정책 읽기를 빼고, 세션 id 를 아는 챗 라우터가 집행 |
| P2 `auto_purge` | `src/housekeeping.py` — 워커 루프가 하루 한 번 보관기간 파기. 파기 사실도 활동 로그에 남는다 |
| P2 기본 프롬프트 서비스명 | `예솜` → `예솜24`(정본은 `web/index.html` `<title>`). `tests/test_service_name.py` |
| P2 복합 질문 진행 표시 | SSE `progress` 이벤트 신설(하위마다 1회). 본문이 아니라 진행 상태라 `done.answer` 에 안 이어붙는다 |

## 판정이 달라진 것

### [P1] 사이드바 URL 불일치 — **재현되지 않음. 결함 아님**

실제 백엔드·로그인 상태에서 사이드바 6회 클릭 + 뒤로/앞으로 3회를 돌려
`location.pathname` · `aria-current="page"` · 화면 제목을 매번 대조했다. **9회 전부 일치했다**
(보고서의 재현 경로 `logs → pipeline → knowledge` 포함).

코드도 이 증상이 나올 구조가 아니다 — `createBrowserRouter` 인스턴스가 하나뿐이고
(`app/router.tsx` → `main.tsx` `RouterProvider`), `AdminLayout.tsx:132` 이 화면 제목·active
메뉴·콘텐츠를 전부 `useLocation()` 에서 파생한다. 별도 상태가 없어 "URL 은 그대로인데 화면만
바뀐다"가 성립할 수 없다.

점검 당시 주소창 스크린샷을 읽는 시점 차이로 보인다. **원인을 모른 채 방어 코드를 넣지 않았다.**

### [P2] selfcheck 5개/6개 불일치 — **이미 고쳐져 있었음**

`web/src/mocks/selfcheck.ts:89` 는 현재 `assert.equal(clar.options.length, 6, ...)` 다.
같은 날 커밋 `4785aa72` 에서 정정됐다 — 점검 시점 이후라 stale 이다.

## 새로 찾은 것

### [P0] 죽은 워커가 남긴 RUNNING 잡이 파이프라인 전체를 막고 있었다

보고서의 "08-24 15:38 변경 감지 · 대기" 를 실제 백엔드에서 확인한 결과 상태는 QUEUED 가 아니라
**RUNNING(`수집:RUNNING`)** 이었다. 워커가 잡을 집은 뒤 프로세스가 사라져 생긴 고아 잡이다.

동시 실행 1개 규칙(`admin_pipeline.ACTIVE_STATUSES`)이 QUEUED 와 RUNNING 을 같이 보므로,
**이 한 행이 이후 모든 작업 생성을 409 로 막고 있었다.** 화면에는 '실행 중'으로 보여 무엇이
막는지도 알기 어렵다. 예외가 아니라 프로세스가 사라진 것이라 `run_job` 의 예외 처리로는 못 잡는다.

→ `worker.reclaim_stale_jobs()` — 워커 기동 시 1시간 넘게 RUNNING 인 잡을 FAILED 로 마감한다
(`created_at` 창을 두는 이유는 워커를 둘 띄웠을 때 남의 잡을 뺏지 않기 위해서다).
현재 DB 대상은 위 1건이며, **API 를 재시작하면 자동으로 정리된다.**

### [P2] `over_limit_message` 도 저장만 되고 안 읽혔다

보고서의 미집행 목록에 없던 세 번째 필드다. AD-009 화면에서 편집·저장되는데 429 응답 문구는
`api/middleware.py` 에 박혀 있어, 관리자가 문구를 바꿔도 사용자는 옛 문구를 봤다. → 정책값 사용.

## 일부러 안 한 것

- **정기 변경 감지 배치** — 외부 사이트를 실제로 크롤하는 작업이라, API 를 띄운 개발 PC 마다
  매일 kdic.or.kr 을 두드리게 된다. 운영 배포 방식이 정해진 뒤에 붙일 것(`src/housekeeping.py` 주석).
- **비동기 export worker** — 상태 4종·저장소·다운로드 URL·만료를 들이는 대신 동기 응답으로
  끝냈다. 대상이 관리자 로그라 최대 수만 행이고, 상한(20,000)을 넘으면 조용히 자르지 않고
  400 으로 되돌린다(`api/export_csv.py` 주석).
- **설정 메뉴 IA(Figma 가로 서브탭 vs 좌측 2단계)** — 제품 결정이라 코드로 판단하지 않았다.

## 검증

```
pytest tests/          459 passed
web  pnpm verify       tsc + oxlint + selfcheck 15종 전체 통과
브라우저(실 백엔드)     STAGE_FAILED 실패 잡 상세 정상 렌더 — "7단계 중 6번째 · 색인 처리 중
                      오류가 발생했습니다" (종전에는 이 잡에서 화면이 통째로 죽었다)
```

⚠️ 실행 중인 8000번 백엔드는 이 수정 **이전에 뜬 프로세스**다. 워커 스레드·CSV 내보내기·
세션 쿼터·고아 잡 정리는 재시작해야 올라온다.

---

# 10. 실환경 재검증 (2026-08-25 17:00~17:10, 새 코드로 재기동한 8000번 기준)

9장의 조치를 **실제 백엔드·실제 브라우저**로 다시 확인했다. 아래는 전부 목(MSW)이 아니라
`VITE_ENABLE_MSW=false` + 실 Supabase 기준이다.

| 항목 | 확인 방법 | 결과 |
|---|---|---|
| P0 실패 상세 | 실패 잡 2건의 [상세]를 각각 열어봄 | 크래시 없음. `STAGE_FAILED` → "6. 색인 (7단계 중 6번째) · 색인 처리 중 오류가 발생했습니다", `INTERNAL` → "1. 수집 (7단계 중 1번째)". `{단계}` 치환 동작 |
| P1 워커 클레임 | inventory 에 없는 page_id 를 가진 QUEUED 잡을 넣어 관찰(네트워크·색인 안 탐) | **+1초 RUNNING · +2초 FAILED** (완료 기준 "3초 안에 RUNNING" 충족). `steps` 는 수집:FAILED + 나머지 SKIPPED. 확인 후 행 삭제 |
| P1 고아 잡 마감 | 2시간 전 `created_at` 의 RUNNING 잡을 넣고 API 재기동 | 기동 직후 FAILED 로 마감 + 로그 `고아 잡 마감: … @청킹 — 워커 중단 추정`. 확인 후 행 삭제 |
| P1 대화 로그 내보내기 | 화면 [내보내기] 클릭 | `conversation-log-exp_….csv` 실제 다운로드. 토스트 "132건" = 화면 "오늘 대화 132건". 파일 데이터 132행 · UTF-8 BOM · 전 행 `+09:00` |
| P1 활동 로그 내보내기 | 사유 입력 후 [내보내기] | `activity-log-exp_….csv` 다운로드. 토스트 "1,023건" = 확인 모달 "1,023건". 파일 1,023행. 직전 대화 로그 내보내기가 감사 기록으로 남아 있는 것도 파일 안에서 확인 |
| P1 사이드바 URL | 메뉴 클릭 6회 + 뒤로/앞으로 3회 | **9/9 전부** `location.pathname` = `aria-current` = 화면 제목 일치 (9장 판정 유지) |
| P2 7단계 문구 | 파이프라인 화면 ⓘ 펼침 | "7단계까지 모두 통과하면 …", 화면에 `6단계` 문자열 없음. 스텝 그림도 7개(게이트 포함) |
| P2 `session_per_30min` | 임시 정책(=1)으로 같은 세션 3회 요청 | 1회 200 → 2·3회 **429**. 확인 후 임시 정책 행 삭제(ops_policy 다시 0행) |
| P2 `over_limit_message` | 위 429 응답 본문 | 정책에 넣은 문구가 그대로 나옴(`"QA 검증용 문구 — 세션 한도에 걸렸습니다"`, `retryable:false`) |
| P2 `auto_purge` | 실 스키마에 `purge_expired_activity_logs` 직접 호출 | 활동 로그 1,158행 중 90일 경과 **0행**(최고령 08-10) → 0 반환, 행 수 불변. 워커 로그에 예외 없음 |
| P2 복합 질문 진행 | SSE 원문 + 화면 관찰 | `accepted → progress(1/2) → delta → progress(2/2) → delta → done`. 화면에 "질문을 2개로 나눠 답하는 중 · 1/2 …" → "2/2 …" 순서로 뜨고 완료 시 사라짐. **델타 합 == done.answer** 불변식 유지, 최상위 `sources` 0 |
| 회귀 | `pytest tests/` · `web pnpm verify` | 459 passed · 자체 점검 전체 통과 |

점검용으로 만든 행(파이프라인 잡 2건, 임시 운영 정책 1건, curl 로 만든 대화 로그)은 전부 삭제해
확인 전 상태로 되돌렸다. 내려받은 CSV 두 개도 지웠다.

## ⚠️ 미완으로 판명된 것 — 서비스명

9장에서 "처리"로 적었지만 **운영에는 아직 반영되지 않는다.**

`src/prompt_builder.py` 는 **게시된 프롬프트가 없을 때만** 쓰이는 기본값이다. 실제로는
`prompt_versions` 에 **v3 가 게시(`is_current`)돼 있고, 그 본문이 아직 `예솜`** 이다 —
오늘 04:54 `admin@demo` 가 게시한 "시스템 프롬프트 검토 반영" 판본이 옛 이름을 그대로 담고 있다.

```
[system]   당신은 예금보험공사(KDIC)의 AI 상담 챗봇 "예솜"입니다. …
[system]   9. … "예금보험공사의 AI 상담 챗봇 예솜"이라고 답하세요.
[few_shot] 안녕하세요! 저는 예금보험공사의 AI 상담 챗봇 예솜입니다. …
```

실제 챗봇에 "너는 누구야?"를 물어본 결과도 `예솜24`·`예솜` 어느 쪽도 말하지 않았다
("예금보험공사(KDIC)의 예금자보호 안내 챗봇입니다").

**게시본은 데이터이고 게시 절차(사유·스모크)가 있는 사람의 작업이라 임의로 바꾸지 않았다.**
AD-008(프롬프트·가드레일)에서 위 세 군데를 `예솜24` 로 고쳐 v4 를 게시하면 끝난다. 그때까지
코드 수정분은 "게시본이 없을 때의 기본값"에만 적용된다.
