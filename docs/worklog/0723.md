# 7월 23일 스탠드업 미팅

## 전일 진행 사항

* RAPTOR RAG의 구조와 현재 프로젝트 적용 가능성 검토

  * 원문 청크와 상위 요약 노드를 함께 검색하는 계층형 검색 방식 확인
  * 현재 프로젝트의 FAQ, 표 데이터, 짧은 안내 페이지에는 구조가 과도할 수 있다고 판단
  * 기존 RAG 파이프라인을 먼저 완성한 뒤 Parent-Child 검색을 우선 검토하기로 결정
* Bi-Encoder와 Cross-Encoder의 차이 및 Reranking 적용 방식 정리
* 기존 검색 및 평가 코드를 역할별 모듈로 분리

```text
src/
├── query_classifier.py
├── retrieval.py
├── reranker.py
├── citation.py
├── civil_petition.py
├── prompt_builder.py
├── llm_client.py
└── pipeline.py
```

* 질문 유형과 답변 의도 분류 구조 구현

  * 질문 유형

    * `table_lookup`
    * `general`
  * 답변 의도

    * `informational`
    * `civil_petition`
* 질문 유형에 따른 검색기 라우팅 구현

  * `table_lookup`: Dense 검색
  * `general`: Dense와 BM25를 결합한 Hybrid 검색
* Cross-Encoder 기반 Reranking 구현

  * `BAAI/bge-reranker-v2-m3` 모델 적용
  * 초기 검색 후보 Top-N을 재정렬한 뒤 최종 Top-K 반환
  * 모델을 요청마다 다시 불러오지 않고 프로세스에서 재사용하도록 구성
* 검색 결과 반환 구조 개선

  * `chunk_id`
  * 본문 텍스트
  * 검색 점수
  * Reranking 점수
* 검색 청크의 출처 정보를 구성하는 Citation 기능 구현

  * `chunk_id`에서 원본 `page_id` 추출
  * 페이지 제목, 브레드크럼, 원문 URL 구성
  * 같은 페이지에서 검색된 출처 중복 제거
  * Reranking 결과 순서 유지
* 민원성 답변에 필요한 데이터 구성 기능 구현

  * 절차 관련 검색 근거
  * 신청 서류 및 양식
  * 관련 공식 페이지
* `attachments`, `form_attachments` 조회 및 중복 제거 로직 구현

  * `sender_docs` 페이지의 첨부파일 28건 중 16건이 중복된 상태임을 확인
  * `(label, url)`을 기준으로 중복 제거
* 정보성 질문과 민원성 질문의 프롬프트 분리
* Few-shot 예시 구성

  * 정보성 질문
  * 민원성 질문
  * 근거 부족 및 범위 밖 질문
* 환각 방지를 위한 시스템 지침 작성

  * 검색 근거에 포함된 내용만 사용
  * 확인할 수 없는 금액, 날짜, 기간, 신청 조건을 임의로 생성하지 않음
  * 제공되지 않은 URL을 새로 생성하지 않음
  * 확인할 수 없는 내용은 자료에서 확인되지 않는다고 안내
* HyperCLOVA X 연결에 필요한 의존성 추가

  * `langchain-naver`
  * `python-dotenv`
* `llm_client.py`에 HyperCLOVA X API 호출 기능 구현
* `.env`와 `.env.example` 파일 구성
* `ChatClovaX` 기본 호출 및 오류 처리 테스트
* `pipeline.py`에서 전체 RAG 모듈 연결

  * 질문 분류
  * 검색기 라우팅
  * Reranking
  * 출처 및 민원 데이터 구성
  * 프롬프트 생성
  * HyperCLOVA X 호출
* 최종 실행 함수 `rag_answer()` 구현
* 정보성 질문과 민원성 질문의 분기 동작 확인
* 터미널 환경에서 전체 RAG 파이프라인 실행 테스트 완료
* 검색 결과를 바탕으로 HyperCLOVA X 답변과 출처가 정상적으로 반환되는 것을 확인
* 실제 민원 질문으로 전체 체인 검증

```text
위임장 양식 어디서 받나요?
```

* 전체 파이프라인은 정상적으로 동작하지만 답변 생성까지 시간이 오래 걸리는 문제 확인
* 구현 내용을 PR로 정리하고 커밋 완료

---

## 금일 진행할 사항

### 1. 전체 파이프라인 응답 속도 측정 및 개선

* `rag_answer()`의 단계별 실행 시간 측정 기능 추가

  * 질문 유형 및 답변 의도 분류
  * Dense·BM25·Hybrid 검색
  * Cross-Encoder Reranking
  * 출처 및 민원 메타데이터 구성
  * 프롬프트 생성
  * HyperCLOVA X API 호출
* 단계별 소요 시간을 로그로 저장하여 주요 병목 구간 확인
* 임베딩 모델, Reranker, BM25 인덱스가 요청마다 다시 로딩되는지 점검
* 모델과 검색 인덱스를 프로세스 시작 시 한 번만 로딩하도록 수정
* Qdrant 연결 객체와 HyperCLOVA X 클라이언트 재사용
* `corpus.jsonl`, `chunks_all.jsonl` 메타데이터 캐싱 상태 점검
* 중복된 파일 읽기와 메타데이터 조회 제거
* 초기 검색 후보 Top-N과 최종 Top-K에 따른 응답 시간 비교
* Reranking 적용 여부에 따른 속도와 검색 성능 비교
* 질문 유형에 따라 Reranking을 선택적으로 적용하는 방식 검토
* HyperCLOVA X에 전달하는 청크의 개수와 전체 프롬프트 길이 조정
* 검색 결과에 포함된 중복 문맥 제거
* API 타임아웃과 재시도 설정으로 인한 불필요한 지연 여부 확인
* 동일한 대표 질문을 사용하여 속도 개선 전후 실행 시간 비교

### 2. 전체 테스트셋 평가 스크립트 작성

* `testset_all.jsonl`의 전체 질문을 `rag_answer()`에 실행하는 `evaluate_pipeline.py` 작성
* 질문별 다음 결과 저장

  * 질문
  * 질문 유형
  * 답변 의도
  * 초기 검색 결과
  * Reranking 결과
  * 최종 답변
  * 사용된 출처
  * 첨부파일
  * 공식 페이지
  * 단계별 실행 시간
  * 오류 발생 여부
* 최종 답변 평가 지표 정의

  * 답변 생성 성공률
  * 출처 포함률
  * 정답 출처 포함률
  * 답변 정확도
  * 근거 부족 질문 거절 성공률
  * 민원 답변 구성 요소 포함률
* 정보성 질문 평가

  * 기준 답변의 핵심 내용 포함 여부
  * 검색 근거와 답변의 일치 여부
  * 잘못된 수치나 조건 생성 여부
* 민원성 질문 평가

  * 절차 안내 포함 여부
  * 신청 서류 포함 여부
  * 공식 페이지 포함 여부
  * 제공되지 않은 URL 생성 여부
* 평가 실패 문항과 오류 원인을 별도 파일로 저장
* 자동 평가가 어려운 문항을 수동 검수 대상으로 구분
* 평가 결과를 JSONL 또는 CSV 형식으로 저장

### 3. RAG 파라미터 튜닝

* 평가 결과를 기준으로 Reranking 적용 전후 성능 비교
* 초기 검색 후보 Top-N 값 비교

  * Top-5
  * Top-10
  * 필요 시 Top-20
* 최종 Top-K 값 비교

  * Top-3
  * Top-5
  * Top-10
* Reranking 적용 여부에 따른 비교

  * Reranking 사용
  * Reranking 미사용
  * 특정 질문 유형에만 Reranking 사용
* 프롬프트 문구 조정

  * 시스템 지침 길이 조정
  * 중복 지침 제거
  * 출처 표시 방식 조정
  * 근거 부족 답변 기준 조정
* Few-shot 적용 여부에 따른 성능 비교
* LLM에 전달하는 청크의 최대 길이 조정
* 동일 페이지의 여러 청크가 검색됐을 때 중복 문맥 제거
* 설정별 다음 항목 비교

  * 답변 정확도
  * 정답 출처 포함률
  * 응답 시간
  * 근거 부족 질문 거절 성공률
* 정확도와 응답 시간을 함께 고려하여 최종 기본 파라미터 확정

### 4. RAG 아키텍처 문서 작성

* `docs/rag_pipeline_architecture.md` 작성
* 전체 RAG 파이프라인 처리 흐름 문서화
* 파일별 역할과 함수 호출 관계 정리
* 현재 파이프라인 구조 기록

```text
사용자 질문
↓
질문 유형 및 답변 의도 분류
↓
검색기 라우팅
├─ table_lookup → Dense
└─ general → Hybrid
↓
초기 후보 Top-N
↓
Cross-Encoder Reranking
↓
최종 Top-K
↓
답변 의도 분기
├─ informational
└─ civil_petition
↓
프롬프트 생성
↓
HyperCLOVA X 호출
↓
답변·출처·첨부파일·공식 페이지 반환
```

* 주요 설계 결정과 근거 기록

  * `table_lookup` 질문을 Dense 검색으로 분기한 이유
  * 일반 질문에 Hybrid 검색을 사용한 이유
  * Cross-Encoder Reranking을 추가한 이유
  * 검색 결과를 페이지가 아닌 청크 단위로 관리한 이유
  * 답변 본문과 출처·첨부파일·URL을 분리한 이유
  * 민원 답변에서 추가 검색을 하지 않고 기존 Top-K를 재사용한 이유
  * 업무 도메인 필터를 제거하거나 제한한 이유
  * Parent-Child 검색을 바로 적용하지 않은 이유
  * RAPTOR RAG를 현재 적용하지 않은 이유
  * LangChain을 전체 구조가 아닌 HyperCLOVA X 호출에 제한적으로 사용한 이유
* 평가 실험 설정과 결과 기록
* 최종 Top-N, Top-K, Reranking 설정 선정 근거 기록
* 현재 한계와 향후 개선 항목 정리

### 5. Streamlit 웹 UI 구현

* 안정화된 `rag_answer()` 함수를 Streamlit에 연결
* 사용자 질문 입력창 구현
* 질문 실행 버튼 구현
* 검색 및 답변 생성 중 로딩 상태 표시
* 최종 답변 본문 출력
* 답변과 출처 영역 분리
* 출처별 다음 정보 표시

  * 페이지 제목
  * 브레드크럼
  * 원문 URL
* 민원 질문의 첨부파일 다운로드 버튼 구현
* 공식 신청 및 안내 페이지 이동 버튼 구현
* 같은 페이지 출처의 중복 표시 방지
* 같은 첨부파일의 중복 표시 방지
* 검색 근거가 부족한 질문의 안내 화면 구현
* 범위 밖 질문의 답변 거절 화면 구현
* HyperCLOVA X API 오류 메시지 표시
* 대표 질문을 이용한 전체 UI 흐름 테스트

  * 정보성 질문
  * 표 조회 질문
  * 민원·신청 질문
  * 첨부파일 요청 질문
  * 근거 부족 질문
  * 범위 밖 질문

---

## 논의 사항

* 전체 응답 지연이 검색, Reranking, HyperCLOVA X 중 어느 단계에서 가장 크게 발생하는지
* Reranking을 모든 질문에 적용할지 특정 질문 유형에만 적용할지
* 응답 속도 개선을 위해 최종 Top-K를 줄일 경우 정확도 저하를 어느 정도까지 허용할지
* 초기 검색 후보 Top-N과 최종 Top-K의 적절한 조합
* 동일 페이지에서 여러 청크가 검색될 경우 LLM에는 몇 개까지 전달할지
* 긴 청크를 그대로 전달할지 필요한 부분만 잘라 전달할지
* 최종 답변 정확도를 자동으로 채점할 구체적인 기준

  * 키워드 기반 평가
  * 기준 답변과의 의미 유사도
  * LLM Judge
  * 수동 검수
* 출처 포함률과 정답 출처 포함률을 별도 지표로 관리할지 여부
* 정답 출처가 여러 개인 문항의 채점 방식
* 민원 질문에서 절차·서류·공식 페이지 중 일부만 검색된 경우의 평가 기준
* Few-shot 예시와 테스트셋 간 데이터 누출 방지 방법
* 근거 부족 여부를 검색 점수로 판단할지 LLM 응답까지 함께 사용할지
* HyperCLOVA X 호출 결과를 평가 과정에서 캐싱할지 여부
* API 호출 실패 시 재시도 횟수와 timeout 기준
* 평가와 Streamlit 구현을 순차적으로 진행할지 병렬로 진행할지
* 아키텍처 문서에 최종 적용 방식뿐 아니라 제외한 방식의 근거도 기록할지 여부

---

# 개인 스탠드업

## 이예주

### Yesterday

* RAPTOR RAG와 Parent-Child 검색 방식 검토
* 현재 데이터 구조에서 RAPTOR 적용 필요성 검토
* RAG 코드 모듈 분리 결과 확인
* 전체 파이프라인 구현 진행 상황 취합
* 정보성 질문과 민원성 질문의 답변 구조 검수
* Few-shot 예시와 평가 데이터 분리 필요성 확인
* HyperCLOVA X 연결 후 터미널 테스트 결과 확인
* 전체 파이프라인의 긴 응답 시간 문제 확인
* PR 설명과 주요 설계 결정 내용 정리

### Today

* `testset_all.jsonl` 문항과 정답 데이터 검수
* 최종 답변 품질 평가 기준 정의
* 정보성·민원성·근거 부족 질문별 평가 항목 작성
* 자동 평가가 어려운 문항의 수동 검수 기준 작성
* 파라미터별 평가 결과 취합
* 설정별 정확도와 응답 시간 비교표 작성
* `docs/rag_pipeline_architecture.md`의 설계 배경 및 의사결정 부분 작성
* 개인별 작업 결과 통합
* Day 3 완료 기준 및 전체 진행 상황 관리

### Blocker

* 답변 정확도를 하나의 자동 평가 지표로 판단하기 어려움
* 정답 문서가 여러 개인 질문의 출처 채점 기준 필요
* 핵심 키워드가 포함되어 있어도 문맥상 잘못된 답변일 수 있어 수동 검수가 일부 필요함

---

## 신동엽

### Yesterday

* 민원성 질문의 절차·서류·공식 페이지 데이터 구성 방식 정리
* `corpus.jsonl`의 `attachments`, `form_attachments` 조회 기능 구현
* 동일한 첨부파일의 중복 상태 확인
* 첨부파일 중복 제거 로직 구현
* Citation 기능을 재사용하여 민원 관련 공식 페이지 구성
* 실제 민원 질문으로 검색부터 민원 컨텍스트 생성까지 검증
* HyperCLOVA X 답변에 서류와 공식 페이지가 포함되는지 확인

### Today

* 민원 질문 평가용 정답 데이터 검수
* 민원 답변의 절차·서류·공식 페이지 포함 여부 자동 검사
* 첨부파일 및 공식 URL 유효성 재검증
* 동일한 출처와 첨부파일이 중복 표시되지 않는지 확인
* 민원 답변 실패 문항 원인 분류
* 민원 답변 프롬프트 길이와 응답 시간 비교
* Streamlit의 첨부파일 다운로드 버튼 데이터 연결
* 공식 신청 및 안내 페이지 버튼 데이터 연결
* 민원 처리 구조의 아키텍처 문서 작성

### Blocker

* 신청 절차가 여러 청크에 나뉜 문항의 평가 기준 필요
* 파일 이름은 다르지만 URL이 동일한 첨부파일의 중복 판단 기준 필요
* 공식 안내 페이지와 실제 신청 페이지를 구분하는 메타데이터가 부족함

---

## 장정훈

### Yesterday

* Cross-Encoder 기반 Reranking 기능 구현
* `BAAI/bge-reranker-v2-m3` 모델 적용
* Reranking 전후 결과 순서 비교
* 검색 결과가 `chunk_id`, 본문, 점수를 포함하도록 구조 수정
* Reranker 모델을 프로세스에서 한 번만 로딩하도록 구성
* 초기 검색 후보와 최종 Top-K 분리 구조 구현
* 전체 파이프라인 터미널 실행 시 검색 및 Reranking 정상 동작 확인

### Today

* `evaluate_pipeline.py` 기본 실행 구조 구현
* `testset_all.jsonl` 전체 문항 반복 실행
* 질문별 초기 검색 및 Reranking 결과 저장
* 검색과 Reranking 단계별 실행 시간 측정
* Reranker가 요청마다 다시 로딩되는지 점검
* Reranking 적용 전후 성능과 응답 시간 비교
* 초기 후보 Top-N 값 실험 자동화
* 최종 Top-3, Top-5, Top-10 결과 비교
* 검색 점수와 실행 시간을 로그로 저장
* 실험 결과를 JSONL 또는 CSV로 저장
* 검색 및 Reranking 구조의 아키텍처 문서 작성

### Blocker

* Reranking 적용 여부와 관계없이 동일한 평가 결과 형식을 유지해야 함
* 전체 테스트셋 실행 시 Reranker와 인덱스가 반복 로딩되지 않도록 구조 점검 필요
* Reranking을 제거했을 때 응답 속도는 개선되지만 검색 정확도가 낮아질 수 있음

---

## 홍주영

### Yesterday

* 정보성 질문과 민원성 질문의 프롬프트 생성 기능 구현
* 환각 방지를 위한 `SYSTEM_INSTRUCTION` 작성
* 정보성·민원성·근거 부족 질문의 Few-shot 예시 구성
* 검색 근거와 출처를 포함한 프롬프트 구조 구현
* URL을 LLM이 임의로 생성하지 못하도록 제한
* `langchain-naver`, `python-dotenv` 의존성 추가
* `llm_client.py`에 HyperCLOVA X API 호출 기능 구현
* `.env`와 `.env.example` 환경변수 파일 구성
* `ChatClovaX` 기본 호출 및 오류 처리 테스트
* `pipeline.py`에서 전체 RAG 모듈 연결
* 최종 실행 함수 `rag_answer()` 구현
* 정보성 질문과 민원성 질문의 분기 동작 확인
* 터미널에서 전체 파이프라인 정상 실행 확인
* 최종 답변과 출처가 정상적으로 반환되는지 검증
* 전체 답변 생성 시간이 오래 걸리는 문제 확인

### Today

* `rag_answer()` 단계별 실행 시간 측정 코드 추가
* HyperCLOVA X 호출 시간과 검색·Reranking 시간을 분리하여 기록
* HyperCLOVA X 클라이언트가 요청마다 다시 생성되는지 확인
* 클라이언트 재사용 구조 적용
* 프롬프트에 전달되는 검색 문맥의 전체 길이 확인
* 중복 검색 문맥 제거
* Top-K와 프롬프트 길이에 따른 응답 시간 비교
* 필요한 규칙은 유지하면서 중복된 시스템 지침 축소
* Few-shot 적용 여부에 따른 응답 시간과 정확도 비교
* API timeout 및 재시도 처리 점검
* 프롬프트 조건별 평가 결과 정리
* LLM 호출 및 프롬프트 구조의 아키텍처 문서 작성

### Blocker

* 전체 지연 중 HyperCLOVA X 호출이 차지하는 비중 측정 필요
* 프롬프트 문맥을 줄일 경우 답변 정확도와 출처 포함률이 낮아질 수 있음
* Few-shot 예시를 제거하면 답변 형식이 불안정해질 가능성이 있음

---

## 장희원

### Yesterday

* 검색 결과와 최종 UI 사이의 응답 데이터 구조 검토
* 답변, 출처, 첨부파일, 공식 페이지를 분리하는 화면 구조 정리
* 정보성 질문과 민원성 질문의 예상 화면 흐름 작성
* 검색 근거가 없는 경우의 안내 화면 구성 검토
* 터미널에서 반환된 `rag_answer()` 결과 구조 확인

### Today

* Streamlit 프로젝트 기본 화면 구현
* 사용자 질문 입력창과 실행 버튼 구성
* `rag_answer()` 함수 연결
* 검색 및 답변 생성 중 로딩 상태 표시
* 답변 본문과 출처 목록 분리 출력
* 민원 서류 다운로드 버튼 구현
* 공식 신청 및 안내 페이지 이동 버튼 구현
* 검색 근거 부족 및 범위 밖 질문 안내 화면 구현
* HyperCLOVA X API 오류 메시지 표시
* 여러 출처와 첨부파일의 중복 제거 및 표시 순서 확인
* 응답 시간이 길어지는 경우 사용자에게 처리 상태가 보이도록 UI 구성
* 대표 질문을 이용한 전체 UI 흐름 테스트

### Blocker

* 평가와 파라미터 튜닝 과정에서 `rag_answer()` 반환 필드가 변경될 가능성이 있음
* 긴 응답 시간 동안 Streamlit 화면이 멈춘 것처럼 보이지 않도록 처리 필요
* 로컬 첨부파일과 외부 URL의 버튼 표시 방식을 구분해야 함

---

## Day 3 완료 기준

### 전체 파이프라인

* [x] HyperCLOVA X API 호출이 정상적으로 실행됨
* [x] `.env`를 통한 API 키 로딩이 정상적으로 동작함
* [x] `pipeline.py`의 `rag_answer()`가 전체 모듈을 연결함
* [x] 정보성 질문과 민원성 질문의 분기가 정상적으로 동작함
* [x] 터미널에서 질문 입력부터 답변과 출처 반환까지 실행됨
* [x] 실제 민원 질문에서 서류와 관련 페이지 데이터가 구성됨

### 응답 속도 개선

* [ ] 파이프라인 단계별 응답 시간이 측정됨
* [ ] 주요 병목 구간이 확인됨
* [ ] 모델과 검색 인덱스가 요청마다 다시 로딩되지 않음
* [ ] HyperCLOVA X 클라이언트가 재사용됨
* [ ] 불필요하게 중복된 검색 문맥이 제거됨
* [ ] 속도 개선 전후의 실행 시간이 비교됨
* [ ] 속도 개선 후에도 답변 정확도와 출처 포함률이 유지됨

### 전체 평가

* [ ] `testset_all.jsonl` 전체 문항이 평가 스크립트에서 실행됨
* [ ] 문항별 최종 답변과 출처가 저장됨
* [ ] 출처 포함률이 계산됨
* [ ] 정답 출처 포함률이 계산됨
* [ ] 답변 정확도 평가 결과가 저장됨
* [ ] 근거 부족 질문의 거절 성공률이 계산됨
* [ ] 민원 답변 구성 요소 포함률이 계산됨
* [ ] 실패 문항과 오류 원인이 별도로 저장됨

### 파라미터 튜닝

* [ ] Reranking 적용 전후 성능과 속도가 비교됨
* [ ] 초기 후보 Top-N 값이 비교됨
* [ ] 최종 Top-3, Top-5, Top-10이 비교됨
* [ ] Few-shot 적용 여부가 비교됨
* [ ] 프롬프트 길이에 따른 응답 시간과 정확도가 비교됨
* [ ] 최종 검색 및 프롬프트 파라미터가 확정됨

### 문서 및 UI

* [ ] `docs/rag_pipeline_architecture.md`가 작성됨
* [ ] 주요 설계 결정과 제외한 방식의 근거가 문서화됨
* [ ] 평가 결과와 최종 파라미터 선정 근거가 기록됨
* [ ] Streamlit에서 질문 입력부터 답변·출처 출력까지 실행됨
* [ ] 민원 질문에서 서류 다운로드와 공식 페이지 이동이 가능함
* [ ] 정보성·민원성·근거 부족 질문의 전체 UI 흐름 테스트가 완료됨
