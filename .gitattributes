# 크로스 플랫폼(맥/윈도우) 일관성.
# .jsonl 은 LF 고정 — 줄바꿈이 CRLF로 바뀌면 임베딩 캐시 해시가 틀어져 공유 벡터를 못 찾는다.
*.jsonl text eol=lf
*.txt text eol=lf
# 바이너리는 git이 손대지 않도록 명시.
*.npy binary
# 크롤 원본 HTML 도 LF 고정 — 재수집이 CRLF 로 저장해 6만 줄 전체가 diff 로 잡히던 문제
# (실제 변화 13줄 → 전체 재작성으로 보임, 2026-08-13 D-7). 검토 가능한 diff 의 전제.
*.html text eol=lf
